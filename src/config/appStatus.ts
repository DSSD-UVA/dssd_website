/**
 * Application status config.
 * Set `applicationsOpen` to true when the application cycle is live.
 * Set `applicationUrl` to the external form link (e.g. Google Form, Airtable).
 */
const appStatus = {
  applicationsOpen: false,
  applicationUrl: 'https://forms.google.com/your-application-form',
  applicationDeadline: 'February 15th',
}

export default appStatus
